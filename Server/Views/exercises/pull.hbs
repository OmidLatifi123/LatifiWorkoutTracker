<h1 class="exH1">Pull Page</h1>
<div class="btnContainer">
    <a href="/exercises/createPul"><button class="createbtn">Create Pull</button></a>
</div>

<div class="container">
    <div class="row justify-content-center">
        {{#each pull}}
        <div class="exCard m-2 text-center col-lg-2">
            <div class="card-body">
                <p class="exP">{{this.reps}}</p>       
                <h6 class="exH6">{{this.title}}</h6>
                <p class="exP">{{{this.description}}}</p>
                <a href="/exercises/editPull/{{this._id}}" class="btn btn-primary">
                    Edit
                </a>
                <a href="/exercises/delete/{{this._id}}" class="btn btn-primary">
                    Delete
                </a>
            </div>
        </div>
        {{/each}}
    </div>
</div>
